#
# ~/.path
#

# ADD `~/bin` TO THE `$PATH`
# export PATH="$HOME/bin:$PATH";

# BASH UTILITIES
PATH="/usr/local/bin:$PATH"
PATH="/usr/local/lib:$PATH"

# HOMEBREW'S CORE CONFIGURATION
PATH="/usr/local/sbin:$PATH"

# HOMEBREW'S `coreutils` — `$(brew --prefix coreutils)/libexec/gnubin`
PATH="/usr/local/opt/coreutils/libexec/gnubin:$PATH"

# HOMEBREW'S CURL --with-openssl
PATH="/usr/local/opt/curl/bin:$PATH"

# MAVEN CONFIGURATION
# M2_HOME="/usr/local/Cellar/apache-maven"
# export MAVEN_OPTS="-Xms1024m -Xmx4096m -XX:PermSize=1024m"
# PATH=$M2_HOME/bin:$PATH

##################################################
# pyenv — "Python Version Manager"               #
##################################################
if which pyenv > /dev/null; then eval "$(pyenv init -)"; fi
# ENABLE AUTO-ACTIVATION OF `pyenv virtualenvs`
if which pyenv > /dev/null; then eval "$(pyenv virtualenv-init -)"; fi

## PATH FOR PYTHON
#PATH=/usr/local/share/python:$PATH
## FOR CUSTOM SCRIPTS
#PATH=$HOME/bin:$PATH
## FOR pyenv
PYENV_ROOT=$HOME/.pyenv
PATH=$PYENV_ROOT/bin:$PATH

##################################################
# rbenv - "Ruby Environment Manager"  #
##################################################
# ADD & LOAD `rbenv` EVERY TIME YOU OPEN A TERMINAL
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi

## FOR rbenv
RBENV_ROOT=$HOME/.rbenv
## FOR CUSTOM SCRIPTS
PATH=$RBENV_ROOT/bin:$PATH


# KEYCLOAK CLI
#KEYCLOAK_HOME=$HOME/dev/mbo/environments/keycloak
#PATH=$KEYCLOAK_HOME/bin:$PATH

# AWS CLI
export PATH="$HOME/.local/bin:$PATH"

# CHEF
# export PATH="/opt/chefdk/bin:$PATH"

# COMPOSER
COMPOSER_HOME=$HOME/.composer
PATH=$COMPOSER_HOME/vendor/bin:$PATH

